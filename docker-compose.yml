services:
  app:
    image: zolwi/devops-for-developers-project-74
    build:
      context: .
      dockerfile: Dockerfile.production
    # Добавь этот блок:
    environment:
      - NODE_ENV=development